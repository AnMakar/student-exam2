pipeline {
    agent any

    stages {
        stage('test') {
            steps {
                sh "python -m venv venv"
		sh ". venv/bin/activate"
		sh "pip install -e '.[test]'"
		sh "coverage run -m pytest"
		sh "coverage report"
            }
        }
    }
}
