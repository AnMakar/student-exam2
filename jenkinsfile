pipeline {
    agent any

    stages {
        stage('test') {
            steps {
                sh ```
                echo 'Hello World'
                cd /opt/student-exam2
                ls -la
                ```
            }
        }
    }
}
