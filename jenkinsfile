pipeline {
    agent any

    stages {
        stage('test') {
            steps {
                sh "echo 'Hello World'"
                sh "cd /opt/student-exam2"
                sh "ls -la /opt"
                ```
            }
        }
    }
}
